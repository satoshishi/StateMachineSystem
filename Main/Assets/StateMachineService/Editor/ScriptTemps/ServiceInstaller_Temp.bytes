using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using StateMachineService.Locator;
using System;

[RequireComponent(typeof(#SERVICE_INSTALLER_NAME#))]
public class #SERVICE_NAME# : MonoBehaviour,IPrefabServiceInstaller
{
    public KeyValuePair<Type,object> Install()
    {
        return new KeyValuePair<Type, object>(
            typeof(#SERVICE_TYPE_NAME#),
            GetComponent<#SERVICE_TYPE_NAME#>()
        );
    }
}
